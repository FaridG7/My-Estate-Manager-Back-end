let express,getToken,setUser;_1c2‍.w("express",[["default",["express"],function(v){express=v}]]);_1c2‍.w("./controller",[["getToken",["getToken"],function(v){getToken=v}],["setUser",["setUser"],function(v){setUser=v}]]);

const cors = require("cors");

const app = express();

const PORT = 4000;
app.use(cors({
  origin: 'http://localhost:5173',
  methods: 'GET, POST, PUT, DELETE',
  credentials: true,
}));
app.use(express.json());

app.post("/login", setUser, getToken);

app.listen(PORT, (err) => {
  if (err) {
    console.log("Error in authServer setup");
  }
  _1c2‍.g.console.log("authServer listening on Port", PORT);
});
