let express,getToken,setManager;_3ef‍.w("express",[["default",["express"],function(v){express=v}]]);_3ef‍.w("./controller",[["getToken",["getToken"],function(v){getToken=v}],["setManager",["setManager"],function(v){setManager=v}]]);

const cors = require("cors");

const app = express();

const PORT = 4000;
app.use(cors({
  origin: 'http://localhost:5173',
  methods: 'GET, POST, PUT, DELETE',
  credentials: true,
}));
app.use(express.json());

app.post("/login", setManager, getToken);

app.listen(PORT, (err) => {
  if (err) {
    console.log("Error in authServer setup");
  }
  _3ef‍.g.console.log("authServer listening on Port", PORT);
});
