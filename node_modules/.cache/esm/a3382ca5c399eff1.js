let express,authenticateToken,managerRoute,peopleRoute,estatesRoute;_c39‍.w("express",[["default",["express"],function(v){express=v}]]);_c39‍.w("../authorization/controller",[["authenticateToken",["authenticateToken"],function(v){authenticateToken=v}]]);_c39‍.w("./manager/managerRoute",[["default",["managerRoute"],function(v){managerRoute=v}]]);_c39‍.w("./people/peopleRoute",[["default",["peopleRoute"],function(v){peopleRoute=v}]]);_c39‍.w("./estates/estatesRoute",[["default",["estatesRoute"],function(v){estatesRoute=v}]]);const cors = require("cors");








const app = express();

const PORT = 3000;

app.use(
  cors({
    origin: "http://localhost:5173",
    methods: "GET, POST, PUT, DELETE",
    credentials: true,
  })
);
app.use(express.json());
app.use(authenticateToken);

app.use("/manager", managerRoute);
app.use("/people", peopleRoute);
app.use("/estates", estatesRoute);

app.listen(PORT, (err) => {
  if (err) {
    console.log("Error in server setup");
  }
  _c39‍.g.console.log("server listening on Port", PORT);
});
