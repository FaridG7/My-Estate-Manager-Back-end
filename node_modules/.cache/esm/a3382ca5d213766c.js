let express,authenticateToken,managerRoute;_d21‍.w("express",[["default",["express"],function(v){express=v}]]);_d21‍.w("../authorization/controller",[["authenticateToken",["authenticateToken"],function(v){authenticateToken=v}]]);_d21‍.w("./manager/managerRoute",[["default",["managerRoute"],function(v){managerRoute=v}]]);


const cors = require("cors");

const app = express();

const PORT = 3000;

app.use(
  cors({
    origin: "http://localhost:5173",
    methods: "GET, POST, PUT, DELETE",
    credentials: true,
  })
);
app.use(express.json());
app.use(authenticateToken);

app.use("/manager", managerRoute);

app.listen(PORT, (err) => {
  if (err) {
    console.log("Error in server setup");
  }
  _d21‍.g.console.log("server listening on Port", PORT);
});
