let queries,jwt;_f1d‍.x([["getUser",()=>getUser]]);_f1d‍.w("./queries.js",[["queries",["queries"],function(v){queries=v}]]);_f1d‍.w("jsonwebtoken-as-promised",[["default",["jwt"],function(v){jwt=v}]]);const pgp = require("pg-promise")();
const db = pgp({
  host: "localhost",
  port: 5432,
  database: "myestatemanager",
  user: "postgres",
  password: "farid.G1382",
});


require("dotenv").config();


       async function getUser(req, res) {
  try {
    const {user} = req;
    const manager = await db.one(queries.getManager, [user.manager_id]);
    res.json(manager) 
  } catch (error) {
    
  }

  

}
